"use server";

import { Header } from "@/components/atoms/server/brand/header/header";

type ContentPageProps = {};

const ContentPage = async ({}: ContentPageProps) => {
    // TODO - TK
    return <Header title={"Content"} />;
};

export default ContentPage;
