"use server";

type RegisterPageProps = {};

const RegisterPage = async ({}: RegisterPageProps) => {
    // TODO - Implement

    return (
        <div>
            {"register"}
        </div>
    );
};

export default RegisterPage;
